/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { Authorization } from './src/containers/authorization/index';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Authorization);
